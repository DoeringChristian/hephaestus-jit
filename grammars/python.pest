alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }

ident = { !digit ~ (alpha | digit)+ }

parameters = { ident ~ ("," ~ ident)* }

function_call_or_sig = { ident ~ "(" ~ parameters ~ ")" }

function_name = { "def" ~ function_call_or_sig }
